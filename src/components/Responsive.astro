<style is:global>
  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html,
  body {
    max-width: 100%;
    overflow-x: hidden;
  }

  img,
  picture,
  video,
  canvas,
  svg {
    max-width: 100%;
  }

  button,
  input,
  select,
  textarea {
    max-width: 100%;
  }

  body .impact-page:not(#responsive-override),
  body .program-page:not(#responsive-override),
  body .site-footer:not(#responsive-override),
  body .program-footer:not(#responsive-override) {
    min-width: 0;
    overflow: clip;
  }

  body .impact-page:not(#responsive-override) h1,
  body .impact-page:not(#responsive-override) h2,
  body .impact-page:not(#responsive-override) h3,
  body .impact-page:not(#responsive-override) p,
  body .program-page:not(#responsive-override) h1,
  body .program-page:not(#responsive-override) h2,
  body .program-page:not(#responsive-override) h3,
  body .program-page:not(#responsive-override) p,
  body .site-footer:not(#responsive-override),
  body .program-footer:not(#responsive-override) {
    max-width: 100%;
    overflow-wrap: break-word;
  }

  @media (max-width: 68.75rem) {
    body .impact-page:not(#responsive-override) .section-shell,
    body .site-footer:not(#responsive-override) .section-shell,
    body .program-page:not(#responsive-override) .program-shell,
    body .program-footer:not(#responsive-override) .program-shell {
      width: min(64rem, calc(100% - 3rem));
    }

    body .hero:not(#responsive-override) .site-header,
    body .program-page:not(#responsive-override) .site-header {
      --nav-gap: clamp(1.25rem, 4vw, 2.75rem);
      right: 0;
      width: 100%;
      padding-right: 1.5rem;
      padding-left: 15rem;
    }

    body .hero:not(#responsive-override) .site-logo,
    body .program-page:not(#responsive-override) .site-logo {
      left: 1.5rem;
      width: 10rem;
    }

    body .hero:not(#responsive-override) .hero__content {
      width: calc(100% - 3rem);
    }

    body .hero:not(#responsive-override) .hero__title {
      margin-inline: 0;
      font-size: clamp(4rem, 10vw, 7rem);
    }

    body .impact-page:not(#responsive-override) .section-shell.mission__grid,
    body .impact-page:not(#responsive-override) .section-shell.highlights__grid,
    body .impact-page:not(#responsive-override) .section-shell.social__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 2rem;
    }

    body .impact-page:not(#responsive-override) .mission__grid > :last-child,
    body .impact-page:not(#responsive-override) .highlights__grid > :last-child,
    body .impact-page:not(#responsive-override) .social__grid > :last-child {
      grid-column: 1 / -1;
      width: min(100%, 31rem);
      justify-self: center;
    }

    body .site-footer:not(#responsive-override) .site-footer__grid,
    body .program-footer:not(#responsive-override) .program-footer__grid {
      grid-template-columns: 1fr 0.7fr 1fr;
      gap: 2.25rem;
    }

    body .program-page:not(#responsive-override) .program-hero h1 {
      font-size: clamp(5rem, 12vw, 7rem);
    }

    body .program-page:not(#responsive-override) .schedule__grid {
      gap: 2rem;
    }

    body .program-page:not(#responsive-override) .event-card {
      grid-template-columns: 7.5rem minmax(0, 1fr);
      gap: 1.1rem;
      padding-inline: 1rem;
    }

    body .program-page:not(#responsive-override) .event-card img {
      width: 7.5rem;
      height: 10rem;
    }

    body .program-page:not(#responsive-override) .program-cta__grid {
      grid-template-columns: minmax(0, 1fr) minmax(17rem, 20rem);
      gap: 2.5rem;
      padding-left: 2.5rem;
    }
  }

  @media (max-width: 47.99rem) {
    body .impact-page:not(#responsive-override) .section-shell,
    body .site-footer:not(#responsive-override) .section-shell,
    body .program-page:not(#responsive-override) .program-shell,
    body .program-footer:not(#responsive-override) .program-shell {
      width: calc(100% - 2rem);
    }

    body .hero:not(#responsive-override),
    body .program-page:not(#responsive-override) .program-hero {
      min-height: 44rem;
    }

    body .hero:not(#responsive-override) {
      height: max(44rem, 100svh);
    }

    body .hero:not(#responsive-override) .hero__image {
      object-position: center center;
    }

    body .hero:not(#responsive-override) .hero__overlay {
      background: rgba(32, 34, 31, 0.52);
    }

    body .hero:not(#responsive-override) .site-header,
    body .program-page:not(#responsive-override) .site-header {
      top: 0;
      right: 0;
      left: 0;
      grid-template-columns: minmax(0, 1fr);
      width: 100%;
      height: 6.75rem;
      padding: 0 1rem;
      column-gap: 0;
      transform: none;
    }

    body .hero:not(#responsive-override) .site-logo,
    body .program-page:not(#responsive-override) .site-logo {
      top: 1.75rem;
      left: 1rem;
      width: clamp(6.75rem, 31vw, 9rem);
    }

    body .hero:not(#responsive-override) .site-nav,
    body .program-page:not(#responsive-override) .site-nav {
      position: absolute;
      top: calc(100% + 0.5rem);
      right: 0;
      display: none;
      width: min(18rem, calc(100vw - 2rem));
      padding: 1.25rem;
      background: rgba(32, 34, 31, 0.94);
      backdrop-filter: blur(12px);
    }

    body .hero:not(#responsive-override) .site-header--open .site-nav,
    body .program-page:not(#responsive-override) .site-header--open .site-nav {
      display: grid;
      gap: 1rem;
    }

    body .hero:not(#responsive-override) .ticket-link,
    body .program-page:not(#responsive-override) .ticket-link {
      display: none;
    }

    body .hero:not(#responsive-override) .menu-button,
    body .program-page:not(#responsive-override) .menu-button {
      display: inline-grid;
      place-content: center;
      justify-self: end;
      width: 3rem;
      height: 3rem;
      padding: 0;
      border: 0;
      border-radius: 50%;
      background: rgba(255, 255, 255, 0.92);
      cursor: pointer;
    }

    body .hero:not(#responsive-override) .menu-button span,
    body .program-page:not(#responsive-override) .menu-button span {
      display: block;
      width: 1.2rem;
      height: 2px;
      margin: 0.17rem 0;
      background: #20221f;
    }

    body .hero:not(#responsive-override) .hero__content {
      width: calc(100% - 2rem);
      padding-bottom: clamp(2rem, 7vh, 4rem);
    }

    body .hero:not(#responsive-override) .hero__title {
      margin: 0 0 clamp(1.4rem, 5vh, 2.4rem);
      font-size: clamp(3.15rem, 16vw, 5.25rem);
      line-height: 0.82;
    }

    body .hero:not(#responsive-override) .hero__description {
      max-width: 27rem;
      margin-bottom: clamp(1.75rem, 5vh, 3rem);
      font-size: clamp(1.1rem, 5vw, 1.45rem);
      line-height: 1.35;
      text-wrap: pretty;
    }

    body .hero:not(#responsive-override) .hero__actions {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.75rem;
      width: min(100%, 27rem);
    }

    body .hero:not(#responsive-override) .button {
      min-width: 0;
      min-height: 3.75rem;
      padding-inline: 1rem;
      font-size: 0.98rem;
    }

    body .impact-page:not(#responsive-override) .mission {
      padding: 3.25rem 0 4rem;
    }

    body .impact-page:not(#responsive-override) .mission__intro .section-label {
      display: block;
      margin-bottom: 1.25rem;
    }

    body .impact-page:not(#responsive-override) .mission h2 {
      font-size: clamp(2.25rem, 12vw, 3.8rem);
      line-height: 1.03;
    }

    body .impact-page:not(#responsive-override) .mission__lead--desktop {
      display: none;
    }

    body .impact-page:not(#responsive-override) .mission__lead--mobile {
      display: block;
      margin-top: 1.25rem;
      font-size: clamp(1.15rem, 5.5vw, 1.6rem);
      line-height: 1.32;
    }

    body .impact-page:not(#responsive-override) .section-shell.mission__grid,
    body .impact-page:not(#responsive-override) .section-shell.highlights__grid,
    body .impact-page:not(#responsive-override) .section-shell.social__grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 1.5rem;
    }

    body .impact-page:not(#responsive-override) .mission__grid {
      margin-top: 3rem;
    }

    body .impact-page:not(#responsive-override) .mission__grid > *,
    body .impact-page:not(#responsive-override) .highlights__grid > *,
    body .impact-page:not(#responsive-override) .social__grid > * {
      grid-column: auto;
      width: 100%;
      min-width: 0;
    }

    body .impact-page:not(#responsive-override) .content-card--theme {
      aspect-ratio: 4 / 3;
    }

    body .impact-page:not(#responsive-override) .content-card--theme .content-card__copy {
      padding: 1.2rem;
    }

    body .impact-page:not(#responsive-override) .content-card--theme .content-card__eyebrow {
      font-size: clamp(1.55rem, 8vw, 2.15rem);
    }

    body .impact-page:not(#responsive-override) .content-card--theme .content-card__description {
      font-size: clamp(1rem, 4.6vw, 1.25rem);
    }

    body .impact-page:not(#responsive-override) .highlights {
      padding: 3.5rem 0 4rem;
    }

    body .impact-page:not(#responsive-override) .highlights__heading .section-label {
      margin-bottom: 1rem;
    }

    body .impact-page:not(#responsive-override) .highlights h2 {
      font-size: clamp(2.35rem, 12vw, 4rem);
      line-height: 1.02;
    }

    body .impact-page:not(#responsive-override) .highlights__heading > p:last-child {
      margin-top: 1rem;
      font-size: 1rem;
    }

    body .impact-page:not(#responsive-override) .highlights__grid {
      margin-top: 3rem;
    }

    body .impact-page:not(#responsive-override) .content-card--highlight .content-card__image {
      aspect-ratio: 4 / 3;
    }

    body .impact-page:not(#responsive-override) .content-card--highlight .content-card__copy {
      padding-top: 1rem;
    }

    body .impact-page:not(#responsive-override) .social {
      padding: 3.5rem 0 4rem;
    }

    body .impact-page:not(#responsive-override) .social h2 {
      font-size: clamp(2.35rem, 12vw, 4rem);
      line-height: 1.03;
    }

    body .impact-page:not(#responsive-override) .social__heading .section-label {
      margin-top: 2rem;
    }

    body .impact-page:not(#responsive-override) .social__grid {
      margin-top: 2.5rem;
    }

    body .impact-page:not(#responsive-override) .content-card--social {
      grid-template-columns: minmax(8.5rem, 42%) minmax(0, 1fr);
      grid-template-rows: none;
      align-items: center;
      gap: 1.25rem;
    }

    body .impact-page:not(#responsive-override) .content-card--social .content-card__image {
      height: auto;
      aspect-ratio: 4 / 5;
    }

    body .impact-page:not(#responsive-override) .content-card--social h3 {
      font-size: clamp(1rem, 5vw, 1.35rem);
      text-align: left;
    }

    body .site-footer:not(#responsive-override),
    body .program-footer:not(#responsive-override) {
      padding: 3.5rem 0 2rem;
    }

    body .site-footer:not(#responsive-override) .site-footer__grid,
    body .program-footer:not(#responsive-override) .program-footer__grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 2.5rem;
    }

    body .site-footer:not(#responsive-override) .site-footer__identity img,
    body .program-footer:not(#responsive-override) .program-footer__identity img {
      margin-bottom: 2rem;
    }

    body .site-footer:not(#responsive-override) .site-footer__legal,
    body .program-footer:not(#responsive-override) .program-footer__legal {
      grid-column: auto;
      margin-top: 0;
    }

    body .program-page:not(#responsive-override) .program-hero {
      height: auto;
      min-height: 42rem;
    }

    body .program-page:not(#responsive-override) .program-hero__image {
      object-position: center 30%;
    }

    body .program-page:not(#responsive-override) .program-hero__content {
      align-self: end;
      padding-top: 9rem;
      padding-bottom: 3.5rem;
    }

    body .program-page:not(#responsive-override) .program-hero__date {
      font-size: clamp(0.85rem, 3.7vw, 1rem);
      line-height: 1.3;
    }

    body .program-page:not(#responsive-override) .program-hero h1 {
      margin-top: 1.25rem;
      font-size: clamp(2.75rem, 14vw, 4.5rem);
      line-height: 0.96;
    }

    body .program-page:not(#responsive-override) .program-hero__lead {
      max-width: 29rem;
      margin-top: 1.75rem;
      font-size: clamp(1.1rem, 5.2vw, 1.45rem);
      line-height: 1.2;
    }

    body .program-page:not(#responsive-override) .program-controls {
      padding: 3rem 0;
    }

    body .program-page:not(#responsive-override) .program-controls__layout {
      grid-template-columns: minmax(0, 1fr);
      row-gap: 2rem;
    }

    body .program-page:not(#responsive-override) .program-controls h2 {
      font-size: clamp(2.5rem, 11vw, 3.5rem);
    }

    body .program-page:not(#responsive-override) .day-tabs,
    body .program-page:not(#responsive-override) .program-filters {
      grid-column: auto;
      justify-content: flex-start;
      flex-wrap: wrap;
      gap: 0.65rem 1.25rem;
    }

    body .program-page:not(#responsive-override) .filter-text {
      font-size: 0.95rem;
    }

    body .program-page:not(#responsive-override) .schedule {
      padding: 3.5rem 0 4rem;
    }

    body .program-page:not(#responsive-override) .schedule__grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 4rem;
    }

    body .program-page:not(#responsive-override) .schedule-day h2 {
      margin-bottom: 2rem;
      font-size: clamp(2.25rem, 10vw, 3.4rem);
    }

    body .program-page:not(#responsive-override) .schedule-day__events {
      gap: 1rem;
    }

    body .program-page:not(#responsive-override) .event-card {
      grid-template-columns: 6.25rem minmax(0, 1fr);
      gap: 1rem;
      min-height: 0;
      padding: 0.75rem;
    }

    body .program-page:not(#responsive-override) .event-card img {
      width: 6.25rem;
      height: 8rem;
      border-width: 4px;
    }

    body .program-page:not(#responsive-override) .event-card h3 {
      margin-top: 0.55rem;
      font-size: 1.1rem;
    }

    body .program-page:not(#responsive-override) .event-card p {
      margin-top: 0.55rem;
      font-size: 0.88rem;
    }

    body .program-page:not(#responsive-override) .festival-pace {
      padding: 3.5rem 0 4rem;
    }

    body .program-page:not(#responsive-override) .festival-pace__image {
      height: clamp(25rem, 112vw, 33rem);
    }

    body .program-page:not(#responsive-override) .festival-pace h2 {
      padding: 2rem 1.25rem;
      font-size: clamp(1.85rem, 8vw, 3rem);
      line-height: 1.08;
    }

    body .program-page:not(#responsive-override) .program-cta {
      padding: 3.5rem 0;
    }

    body .program-page:not(#responsive-override) .program-cta__grid {
      grid-template-columns: minmax(0, 1fr);
      gap: 2.5rem;
      min-height: 0;
      padding: 1.5rem;
    }

    body .program-page:not(#responsive-override) .program-cta .program-label {
      font-size: 0.9rem;
    }

    body .program-page:not(#responsive-override) .program-cta h2 {
      margin-top: 1rem;
      font-size: clamp(2.45rem, 11vw, 4rem);
    }

    body .program-page:not(#responsive-override) .program-cta__copy > p:not(.program-label) {
      margin-top: 1.25rem;
      font-size: 1rem;
    }

    body .program-page:not(#responsive-override) .program-cta__actions {
      margin-top: 2rem;
    }

    body .program-page:not(#responsive-override) .program-cta__grid > img {
      height: auto;
      aspect-ratio: 4 / 5;
    }
  }

  @media (max-width: 30rem) {
    body .impact-page:not(#responsive-override) .section-shell,
    body .site-footer:not(#responsive-override) .section-shell,
    body .program-page:not(#responsive-override) .program-shell,
    body .program-footer:not(#responsive-override) .program-shell {
      width: calc(100% - 1.5rem);
    }

    body .hero:not(#responsive-override) .hero__content {
      width: calc(100% - 1.5rem);
    }

    body .hero:not(#responsive-override) .site-header,
    body .program-page:not(#responsive-override) .site-header {
      right: 0;
      width: 100%;
      padding-inline: 0.75rem;
    }

    body .hero:not(#responsive-override) .site-logo,
    body .program-page:not(#responsive-override) .site-logo {
      left: 0.75rem;
    }

    body .hero:not(#responsive-override) .hero__actions {
      grid-template-columns: minmax(0, 1fr);
      gap: 0.65rem;
    }

    body .hero:not(#responsive-override) .button {
      width: 100%;
    }

    body .impact-page:not(#responsive-override) .mission h2,
    body .impact-page:not(#responsive-override) .highlights h2,
    body .impact-page:not(#responsive-override) .social h2 {
      font-size: clamp(2rem, 11.5vw, 3.1rem);
    }

    body .impact-page:not(#responsive-override) .content-card--social {
      grid-template-columns: minmax(0, 1fr);
    }

    body .impact-page:not(#responsive-override) .content-card--social .content-card__image {
      width: 100%;
      aspect-ratio: 4 / 5;
    }

    body .impact-page:not(#responsive-override) .content-card--social h3 {
      text-align: center;
    }

    body .program-page:not(#responsive-override) .program-hero h1 {
      font-size: clamp(2.65rem, 13vw, 3.75rem);
    }

    body .program-page:not(#responsive-override) .event-card {
      grid-template-columns: 5.5rem minmax(0, 1fr);
      gap: 0.75rem;
    }

    body .program-page:not(#responsive-override) .event-card img {
      width: 5.5rem;
      height: 7.25rem;
    }

    body .program-page:not(#responsive-override) .program-button {
      width: 100%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      scroll-behavior: auto !important;
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }
</style>
