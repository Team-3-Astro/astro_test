<style is:global>
	* {
		box-sizing: border-box;
	}

	html,
	body {
		overflow-x: hidden;
	}

	body {
		margin: 0;
	}

	img {
		max-width: 100%;
	}

	@media (max-width: 1100px) {
		.section-shell,
		.program-shell {
			width: calc(100% - 3rem);
		}

		.site-header {
			width: calc(100% - 3rem);
		}

		.impact-page .mission h2,
		.impact-page .highlights h2,
		.impact-page .social h2 {
			font-size: 3.75rem;
		}

		.impact-page .mission__lead {
			font-size: 1.7rem;
		}

		.impact-page .mission__grid,
		.impact-page .highlights__grid,
		.impact-page .social__grid {
			gap: 1.5rem;
		}

		.program-page .program-hero h1 {
			font-size: 6.5rem;
		}

		.program-page .program-hero__lead {
			font-size: 1.5rem;
		}

		.program-page .event-card {
			grid-template-columns: 7rem minmax(0, 1fr);
			padding-inline: 0.75rem;
		}

		.program-page .event-card img {
			width: 7rem;
			height: 10.5rem;
		}

		.program-page .program-cta__grid {
			grid-template-columns: minmax(0, 1fr) 19rem;
			padding-left: 2.5rem;
		}
	}

	@media (max-width: 760px) {
		.section-shell,
		.program-shell {
			width: calc(100% - 3rem);
		}

		.site-header {
			top: 1rem;
			left: 1rem;
			grid-template-columns: 1fr auto;
			width: calc(100% - 2rem);
			height: 3.875rem;
			padding: 0.5rem;
			transform: none;
			background: rgba(32, 34, 31, 0.08);
			backdrop-filter: blur(9px);
		}

		.site-header .site-logo {
			width: 6.95rem;
		}

		.site-header .site-nav,
		.site-header .ticket-link {
			display: none;
		}

		.site-header .menu-button {
			display: grid;
			place-items: center;
			justify-self: end;
			width: 2.875rem;
			height: 2.875rem;
			padding: 0;
			border: 0;
			border-radius: 50%;
			background: #ffffff;
			cursor: pointer;
		}

		.site-header .menu-button span {
			position: absolute;
			width: 1.25rem;
			height: 2px;
			border-radius: 1px;
			background: #20221f;
			transition: transform 160ms ease;
		}

		.site-header .menu-button span:first-child {
			transform: translateY(-0.34rem);
		}

		.site-header .menu-button span:last-child {
			transform: translateY(0.34rem);
		}

		.site-header--open .menu-button span:first-child {
			transform: rotate(45deg);
		}

		.site-header--open .menu-button span:last-child {
			transform: rotate(-45deg);
		}

		.site-header--open .site-nav {
			position: absolute;
			top: 4.5rem;
			right: 0;
			display: grid;
			gap: 1rem;
			min-width: 12rem;
			padding: 1.25rem;
			border-radius: 0.5rem;
			background: rgba(32, 34, 31, 0.96);
		}

		.hero {
			height: 15.75rem;
			min-height: 0;
		}

		.hero .hero__image {
			object-position: center 48%;
		}

		.hero .hero__overlay,
		.hero .hero__content {
			display: none;
		}

		.impact-page .mission {
			padding: 2rem 0 4.9rem;
		}

		.impact-page .mission__intro {
			text-align: left;
		}

		.impact-page .mission__intro .section-label {
			display: block;
			margin-bottom: 1rem;
		}

		.impact-page .mission h2,
		.impact-page .highlights h2,
		.impact-page .social h2 {
			font-size: 2.625rem;
			line-height: 1.04;
		}

		.impact-page .mission__lead {
			margin-top: 1rem;
			font-size: 1.125rem;
			line-height: 1.42;
		}

		.impact-page .mission__lead--desktop {
			display: none;
		}

		.impact-page .mission__lead--mobile {
			display: block;
		}

		.impact-page .mission__grid,
		.impact-page .highlights__grid,
		.impact-page .social__grid {
			grid-template-columns: 1fr;
		}

		.impact-page .mission__grid {
			gap: 2.25rem;
			margin-top: 0.75rem;
		}

		.impact-page .content-card--theme {
			aspect-ratio: 1.03 / 1;
		}

		.impact-page .content-card--theme .content-card__copy {
			padding: 1rem;
		}

		.impact-page .content-card--theme .content-card__eyebrow {
			font-size: 2rem;
		}

		.impact-page .content-card--theme .content-card__description {
			max-width: 19rem;
			font-size: 1.25rem;
		}

		.impact-page .highlights {
			padding: 2.4rem 0 0;
		}

		.impact-page .highlights__heading .section-label {
			margin-bottom: 1rem;
			text-align: center;
		}

		.impact-page .highlights__heading > p:last-child {
			display: none;
		}

		.impact-page .highlights__grid {
			gap: 0;
			margin-top: 3.5rem;
		}

		.impact-page .content-card--highlight {
			min-height: 24.4rem;
		}

		.impact-page .content-card--highlight .content-card__image {
			aspect-ratio: auto;
			height: 14.4rem;
		}

		.impact-page .content-card--highlight .content-card__copy {
			padding: 1.75rem 1.1rem 2rem;
		}

		.impact-page .content-card--highlight h3 {
			font-size: 1.2rem;
		}

		.impact-page .content-card--highlight .content-card__description {
			font-size: 1.1rem;
		}

		.impact-page .social {
			padding: 2rem 0 3.75rem;
		}

		.impact-page .social__heading {
			text-align: left;
		}

		.impact-page .social__heading .section-label {
			margin-top: 0.5rem;
		}

		.impact-page .social__heading > a {
			margin-top: 1rem;
			font-size: 0.875rem;
		}

		.impact-page .social__grid {
			gap: 1.5rem;
			margin-top: 2.2rem;
		}

		.impact-page .content-card--social {
			grid-template-columns: 10.1rem minmax(0, 1fr);
			grid-template-rows: 13.25rem;
			align-items: center;
			gap: 1rem;
		}

		.impact-page .content-card--social .content-card__image {
			width: 10.1rem;
			height: 13.25rem;
			aspect-ratio: auto;
		}

		.impact-page .content-card--social h3 {
			font-size: 1.05rem;
			line-height: 1.25;
			text-align: left;
		}

		.site-footer,
		.program-footer {
			padding: 2.5rem 0 1.25rem;
		}

		.site-footer .site-footer__grid,
		.program-footer .program-footer__grid {
			grid-template-columns: 1fr 1.15fr;
			gap: 2rem;
		}

		.site-footer .site-footer__identity,
		.program-footer .program-footer__identity {
			grid-column: 1 / -1;
		}

		.site-footer .site-footer__identity img,
		.program-footer .program-footer__identity img {
			width: 7.25rem;
			margin-bottom: 4.25rem;
		}

		.site-footer nav a,
		.site-footer .newsletter label,
		.program-footer nav a,
		.program-footer .program-newsletter label {
			font-size: 0.75rem;
		}

		.site-footer .newsletter input,
		.program-footer .program-newsletter input {
			width: 100%;
			min-height: 2.75rem;
			font-size: 0.75rem;
		}

		.site-footer .newsletter button,
		.program-footer .program-newsletter button {
			min-width: 7.25rem;
			min-height: 2.625rem;
		}

		.site-footer .site-footer__legal,
		.program-footer .program-footer__legal {
			margin-top: 0;
			font-size: 0.625rem;
		}

		.program-page .program-hero {
			height: 40.625rem;
		}

		.program-page .program-hero__image {
			top: 6.9rem;
			left: -6.9rem;
			width: 32.5rem;
			height: 40.625rem;
			object-position: center;
		}

		.program-page .program-hero__content {
			padding-top: 8rem;
		}

		.program-page .program-hero__date {
			font-size: 0.75rem;
		}

		.program-page .program-hero h1 {
			margin-top: 1.35rem;
			color: #ffffff;
			font-size: 3.625rem;
			font-weight: 400;
			line-height: 1;
		}

		.program-page .program-hero__lead {
			max-width: 22rem;
			margin-top: 2rem;
			color: #ffffff;
			font-size: 1.0625rem;
			font-weight: 400;
			line-height: 1.47;
		}

		.program-page .program-controls {
			padding: 3rem 0 2.25rem;
			background: #ffffff;
		}

		.program-page .program-controls__layout {
			grid-template-columns: 1fr;
			row-gap: 2rem;
		}

		.program-page .program-controls .program-label::before,
		.program-page .festival-pace .program-label::before {
			display: inline-block;
			width: 0.5rem;
			height: 0.5rem;
			margin-right: 0.75rem;
			border-radius: 0.25rem;
			background: #d83b9c;
			content: '';
		}

		.program-page .program-controls h2 {
			margin-top: 1.6rem;
			font-size: 2.5rem;
		}

		.program-page .day-tabs {
			justify-content: flex-start;
			gap: 2rem;
			margin-top: 2.5rem;
		}

		.program-page .program-filters {
			grid-column: auto;
			flex-wrap: wrap;
			gap: 0.4rem 1rem;
		}

		.program-page .filter-text {
			font-size: 0.8125rem;
			font-weight: 500;
		}

		.program-page .filter-text:not(.filter-text--active) {
			color: #85887f;
		}

		.program-page .schedule {
			padding: 3rem 0 4.5rem;
			background: #eaeae4;
		}

		.program-page .schedule__grid {
			grid-template-columns: 1fr;
			gap: 3.5rem;
		}

		.program-page .schedule-day h2 {
			margin-bottom: 3.25rem;
			font-size: 2.1875rem;
			font-weight: 400;
		}

		.program-page .schedule-day__events {
			gap: 1.5rem;
		}

		.program-page .event-card {
			display: block;
			min-height: 13.375rem;
			padding: 1.1rem;
			background: rgba(255, 255, 255, 0.48);
		}

		.program-page .event-card img {
			display: none;
		}

		.program-page .event-card time {
			font-size: 0.8125rem;
		}

		.program-page .event-card h3 {
			margin-top: 1.5rem;
			color: #20221f;
			font-size: 1.1875rem;
			font-weight: 500;
		}

		.program-page .event-card p {
			margin-top: 1.5rem;
			color: #85887f;
			font-size: 0.875rem;
			font-weight: 400;
		}

		.program-page .festival-pace {
			padding: 2.65rem 0 3.4rem;
			background: #ffffff;
		}

		.program-page .festival-pace .program-label {
			margin-bottom: 1.5rem;
		}

		.program-page .festival-pace__image {
			height: 25rem;
		}

		.program-page .festival-pace__image img {
			object-position: 45% 43%;
		}

		.program-page .festival-pace h2 {
			padding: 2.1rem 1.25rem;
			font-size: 1.8125rem;
		}

		.program-page .program-cta {
			padding: 1.9rem 0 2.5rem;
			color: #20221f;
		}

		.program-page .program-cta__grid {
			grid-template-columns: 1fr;
			gap: 0;
			min-height: 28.75rem;
			padding: 1.9rem 1.5rem;
			background: #ffffff;
		}

		.program-page .program-cta .program-label {
			font-size: 0.75rem;
		}

		.program-page .program-cta h2 {
			margin-top: 1.5rem;
			color: #20221f;
			font-size: 2.375rem;
			font-weight: 400;
		}

		.program-page .program-cta__copy > p:not(.program-label) {
			margin-top: 3.2rem;
			font-size: 0.9375rem;
			line-height: 1.45;
		}

		.program-page .program-cta__actions {
			align-items: flex-start;
			flex-direction: column;
			gap: 0.75rem;
			margin-top: 3.6rem;
		}

		.program-page .program-button {
			min-height: 3rem;
			font-size: 0.9375rem;
		}

		.program-page .program-button--accent {
			background: rgba(240, 68, 46, 0.8);
		}

		.program-page .program-button--outline {
			border: 1px solid rgba(240, 68, 46, 0.6);
			color: #f0442e;
		}

		.program-page .program-cta__grid > img {
			display: none;
		}

		.program-footer {
			background: #20221f;
			color: #ffffff;
		}
	}
</style>
